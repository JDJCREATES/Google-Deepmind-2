/**
 * Preview Port Utilities
 * 
 * NOTE: Port values are now fetched directly from the backend via /preview/status
 * This ensures RunCard and ProcessDashboard always show the same value.
 * The backend (preview_manager) is the single source of truth for ports.
 */
