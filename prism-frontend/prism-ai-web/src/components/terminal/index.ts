/**
 * Terminal Components Index
 * 
 * Exports both the simple output-only terminal and the interactive xterm terminal.
 */

export { Terminal, type TerminalLine } from '../Terminal';
export { XTerminal } from './XTerminal';
